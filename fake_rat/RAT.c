#include <windows.h>

int WINAPI WinMain(HINSTANCE hInstance,
                   HINSTANCE hPrevInstance,
                   LPSTR lpCmdLine,
                   int nCmdShow) {

  MessageBox(NULL, "Remote Access Toolkit Installed", "Totally Legit Malware Installer", MB_OK | MB_ICONINFORMATION);

  return 0;
}
